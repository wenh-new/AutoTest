<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 命名空间mapper,如果有多个mapper文件，这个必须唯一 -->
<mapper namespace="com.course.model">
    <resultMap id="userMap" type="com.course.model.User">
        <id property="id" column="id"/>
        <result property="userName" column="userName"/>
        <result property="password" column="password"/>
        <result property="age" column="age"/>
        <result property="sex" column="sex"/>
        <result property="permission" column="permission"/>
        <result property="isDelete" column="isDelete"/>
    </resultMap>
    <!--获取登陆接口case-->
    <select id="loginCase" parameterType="Integer"  resultType="com.course.model.LoginCase">
        <!-- SQL语句 -->
        select * from loginCase
        where id = #{id};
    </select>
    <!--添加用户接口case-->
    <select id="addUserCase" parameterType="Integer" resultType="com.course.model.AddUserCase">
        <!-- SQL语句 -->
        select * from addUserCase where id=#{id};
    </select>

    <!--获取用户信息case-->
    <select id="getUserInfoCase" parameterType="Integer" resultType="com.course.model.GetUserInfoCase">
        <!-- SQL语句 -->
        select * from getUserInfoCase where id=#{id};
    </select>

    <!--获取用户列表case-->
    <select id="getUserListCase" parameterType="Integer" resultType="com.course.model.GetUserListCase">
        <!-- SQL语句 -->
        select * from getUserListCase where id=#{id};
    </select>

    <!--更新/删除用户信息case-->
    <select id="updateUserInfoCase" parameterType="Integer" resultType="com.course.model.UpdateUserInfoCase">
        <!-- SQL语句 -->
        select * from updateUserInfoCase where id = #{id};
    </select>
<!--分割线-->
    <!--获取添加用户之后的数据-->
    <select id="addUser" parameterType="com.course.model.AddUserCase" resultMap="userMap">
        select * from user where
        userName=#{userName}
        and password=#{password}
        and sex=#{sex}
        and age=#{age}
        and permission=#{permission}
        and isDelete=#{isDelete};
    </select>

    <!--获取用户信息-->
    <select id="getUserInfo" parameterType="com.course.model.GetUserInfoCase" resultMap="com.course.model.userMap">
        select * from user where
        id =#{userId}
    </select>
    <!--获取用户列表-->
    <select id="getUserList" parameterType="com.course.model.GetUserListCase" resultMap="com.course.model.userMap">
        select * from user
        <trim prefix="WHERE" prefixOverrides="and">
            <if test="null !=userName and '' !=userName">
                AND userName=#{userName}
            </if>
            <if test="null !=sex and '' !=sex">
                AND sex=#{sex}
            </if>
            <if test="null !=age and '' !=age">
                AND age=#{age}
            </if>
        </trim>
        ;
    </select>
    <!--获取更新之后的数据-->
    <select id="getUpdateUserInfo" parameterType="com.course.model.UpdateUserInfoCase" resultMap="com.course.model.userMap">
        select * from user
        <trim prefix="WHERE" prefixOverrides="and" >
            <if test="null !=age and ''!=age">
                AND age=#{age}
            </if>
            <if test="null !=userName and ''!=userName">
                AND userName=#{userName}
            </if>
            <if test="null !=sex and ''!=sex">
                AND sex=#{sex}
            </if>
            <if test="null !=permission and ''!=permission">
                AND permission=#{permission}
            </if>
            <if test="null !=isDelete and ''!=isDelete">
                AND isDelete=#{isDelete}
            </if>
        </trim>
        And id = #{userId}
    </select>




    <!--&lt;!&ndash;添加用户接口&ndash;&gt;-->
    <!--<select id="addUser" parameterType="com.course.model.AddUserCase" resultType="com.course.model.User">-->
         <!--SQL语句 -->
        <!--select * from user where-->
        <!--userName=#{userName}-->
        <!--and password=#{password}-->
        <!--and sex=#{sex}-->
        <!--and age=#{age}-->
        <!--and permission=#{permission}-->
        <!--and isDelete=#{isDelete};-->

    <!--</select>-->

    <!--&lt;!&ndash;获取用户信息&ndash;&gt;-->
    <!--<select id="getUserInfo" parameterType="com.course.model.GetUserInfoCase" resultType="com.course.model.User">-->
        <!--&lt;!&ndash; SQL语句 &ndash;&gt;-->
        <!--select * from user where-->
        <!--id=#{userId};-->
    <!--</select>-->

    <!--&lt;!&ndash;获取用户列表&ndash;&gt;-->
    <!--<select id="getUserList" parameterType="com.course.model.GetUserListCase" resultType="com.course.model.User">-->
        <!--&lt;!&ndash; SQL语句 &ndash;&gt;-->
        <!--select * from user-->
        <!--<trim prefix="WHERE" prefixOverrides="and">-->
            <!--<if test="null != userName and '' !=userName">-->
                <!--AND userName=#{userName}-->
            <!--</if>-->
            <!--<if test="null != sex and '' !=sex">-->
                <!--AND sex=#{sex}-->
            <!--</if>-->
            <!--<if test="null != age and '' !=age">-->
                <!--AND age=#{age}-->
            <!--</if>-->

        <!--</trim>-->
        <!--;-->
    <!--</select>-->

    <!--&lt;!&ndash;获取更新后的数据&ndash;&gt;-->
    <!--<select id="getUpdateUserInfo" parameterType="com.course.model.UpdateUserInfoCase" resultType="com.course.model.User">-->
        <!--&lt;!&ndash; SQL语句 &ndash;&gt;-->
        <!--select * from user-->
        <!--<trim prefix="WHERE" prefixOverrides="and">-->
            <!--<if test="null != userName and '' !=userName">-->
                <!--AND userName=#{userName}-->
            <!--</if>-->
            <!--<if test="null != sex and '' !=sex">-->
                <!--AND sex=#{sex}-->
            <!--</if>-->
            <!--<if test="null != age and '' !=age">-->
                <!--AND age=#{age}-->
            <!--</if>-->
            <!--<if test="null != permission and '' !=permission">-->
                <!--AND permission=#{permission}-->
            <!--</if>-->
            <!--<if test="null != isDelete and '' !=isDelete">-->
                <!--AND isDelete=#{isDelete}-->
            <!--</if>-->
        <!--</trim>-->
        <!--And id = #{userId};-->
    <!--</select>-->


</mapper>